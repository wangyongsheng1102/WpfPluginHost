<Window x:Class="ShellApp.Views.AuthorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        Title="{Binding WindowTitle}"
        Height="1240" Width="1040"
        MinHeight="1240" MinWidth="1040"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="False"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource ShellBackgroundBrush}">
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0,0,0,1" CornerRadius="0" CaptionHeight="104" ResizeBorderThickness="8" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="104" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding TitleBarText}"
                   VerticalAlignment="Center" Margin="48,0,0,0"
                   Style="{StaticResource SubtitleTextStyle}"
                   Foreground="{DynamicResource PrimaryTextBrush}"
                   IsHitTestVisible="False" />

        <ScrollViewer Grid.Row="1" Margin="48,0,64,24"
                      Padding="0,0,16,0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Border Background="{DynamicResource ContentBackgroundBrush}"
                        BorderBrush="{DynamicResource BorderSubtleBrush}"
                        BorderThickness="1" CornerRadius="8"
                        Padding="48" Margin="0,0,0,48">
                    <StackPanel>
                        <TextBlock Text="{Binding ProductName}" Style="{StaticResource TitleTextStyle}" FontSize="52" />
                        <TextBlock Text="{Binding VersionDisplay}" Style="{StaticResource BodyTextStyle}" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,8,0,40" />

                        <StackPanel Orientation="Horizontal">
                            <svgc:SvgViewbox Source="/Assets/SVG/avatar-bad-breaking-svgrepo-com.svg"
                                             Width="88" Height="88"
                                             Stretch="Uniform"
                                             VerticalAlignment="Center"
                                             Margin="0,0,32,0" />
                            <StackPanel VerticalAlignment="Center">
                                <TextBlock Text="{Binding DeveloperName}" Style="{StaticResource SubtitleTextStyle}" />
                                <TextBlock Text="{Binding DeveloperTeam}" Style="{StaticResource BodyTextStyle}" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,4,0,0" />
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <TextBlock Text="{Binding ReleaseNotesHeading}"
                           Style="{StaticResource SubtitleTextStyle}"
                           Margin="8,0,0,32" />

                <ItemsControl ItemsSource="{Binding ReleaseNotes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,32"
                                    Background="{DynamicResource NavBackgroundBrush}"
                                    BorderBrush="{DynamicResource BorderSubtleBrush}"
                                    CornerRadius="8"
                                    Padding="40">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderThickness" Value="1" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsHighlight}" Value="True">
                                                <Setter Property="BorderThickness" Value="2" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <Grid Margin="0,0,0,24">
                                        <TextBlock Text="{Binding VersionTitle}"
                                                   FontFamily="{DynamicResource AppFontFamilyBold}"
                                                   FontWeight="Normal" FontSize="30" Foreground="{DynamicResource PrimaryTextBrush}" HorizontalAlignment="Left" />
                                        <TextBlock Text="{Binding DateLabel}" Style="{StaticResource CaptionTextStyle}" HorizontalAlignment="Right" />
                                    </Grid>
                                    <TextBlock Text="{Binding Notes}"
                                               Style="{StaticResource CaptionTextStyle}" Foreground="{DynamicResource PrimaryTextBrush}" LineHeight="44" TextWrapping="Wrap" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2"
                Content="{Binding CloseButtonText}"
                Width="280"
                Height="80"
                Margin="0,0,0,36"
                Style="{DynamicResource PrimaryButtonStyle}"
                Command="{Binding CloseCommand}"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom" />
    </Grid>
</Window>
